public class Granny extends Person implements Moveable {
    Granny(int age, int handPosition, int legPosition, int headPosition){
        super(age, handPosition, legPosition, headPosition);
    }
}
